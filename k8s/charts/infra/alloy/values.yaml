# Grafana Alloy log shipper for the T1 observability stack (local/dev k8s only).
image:
  repository: grafana/alloy
  tag: "v1.7.4"
  pullPolicy: IfNotPresent
loki:
  # Push endpoint -- the in-cluster Loki service (release name esquire-infra-loki).
  endpoint: http://esquire-infra-loki:3100/loki/api/v1/push
