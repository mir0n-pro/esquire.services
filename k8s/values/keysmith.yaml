# Local k8s overlay for charts/esquire-keysmith
# Chart defaults already match local dev needs. Only image.tag is
# restamped per rebuild here -- k8s-rebuild.bat patches it.
image:
  tag: "v1.2.8-2606.1518"
