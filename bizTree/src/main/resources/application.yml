spring:
  profiles:
    active: ${DB_BIZTREE_VENDOR:dev-postgres},${BIZTREE_CACHE_VENDOR:cache-h2}
  application:
    name: biztree
  activemq:
    broker-url: ${AMQ_BROKER_URL:tcp://localhost:61616}
    user: ${AMQ_USER:}
    password: ${AMQ_PASSWORD:}
    # Phase 1: no broker authorization required
  jms:
    # clientId on CachingConnectionFactory — required for durable subscriptions.
    # Must not be set on DefaultJmsListenerContainerFactory (shared connection proxy restriction).
    client-id: ${BIZTREE_MESSAGING_CLIENT_ID:biztree}
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
  mvc:
    problemdetails:
      enabled: true
# Messaging: entity broadcast consumer
biztree:
  h2:
    # H2 in-memory tree cache — independent from esquireDB
    url: ${BIZTREE_H2_URL:jdbc:h2:mem:biztree;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE}
    pool:
      pool-name:            biztree-h2-cache
      maximum-pool-size:    ${BIZTREE_H2_POOL_MAX:10}
      minimum-idle:         ${BIZTREE_H2_POOL_MIN_IDLE:10}
      connection-timeout:   ${BIZTREE_H2_POOL_CONN_TIMEOUT:5000}
      max-lifetime:         ${BIZTREE_H2_POOL_MAX_LIFETIME:1800000}
      idle-timeout:         ${BIZTREE_H2_POOL_IDLE_TIMEOUT:600000}
  messaging:
    # Stable client ID for durable subscriptions from this service
    client-id: ${BIZTREE_MESSAGING_CLIENT_ID:biztree}
    consumer:
      enabled: ${BIZTREE_MESSAGING_CONSUMER_ENABLED:true}
    # Dedicated log file for received entity broadcast messages
    broadcast-log-path: ${BIZTREE_BROADCAST_LOG_PATH:logs/entity-broadcast.log}
logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss, UTC} %-5level [%X{correlationId:-no-cid}] [%X{requestId:-no-rid}] %logger{36} - %msg%n"
  structured:
    format:
      console: ecs  # Use ecs,'logstash' or 'gelf' for other common formats
  level:
    root: ${LOG_LEVEL_ROOT:ERROR}
    org:
      springframework: ${LOG_LEVEL_SF:ERROR}
      springframework.jms: ${LOG_LEVEL_JMS:ERROR}
      apache:
        activemq: ${LOG_LEVEL_AMQ:ERROR}
    pro:
      mir0n: ${LOG_LEVEL_MIR0N:INFO}
    entity:
      broadcast: ${LOG_LEVEL_BROADCAST:INFO}

---
# Development Oracle
spring:
  config:
    activate:
      on-profile: dev-oracle
  datasource:
    url: jdbc:oracle:thin:@//${DB_BIZTREE_HOST:localhost}:${DB_BIZTREE_PORT:1521}/${DB_BIZTREE_NAME:MIR0N}
    driverClassName: oracle.jdbc.OracleDriver
    username: ${DB_BIZTREE_USERNAME:esq2025}
    password: ${DB_BIZTREE_PASSWORD:q}   
    hikari:
      maximum-pool-size: 20
      minimum-idle: 20
      connection-timeout: 30000 
      max-lifetime: 1800000 
      idle-timeout: 600000    
  jpa:
    mapping-resources:
      - META-INF/oracle-entity.xml
    properties:
      hibernate:
        dialect: org.hibernate.dialect.OracleDialect
server:
  port: ${BIZTREE_PORT:3000}

---
# Development Postgres
spring:
  config:
    activate:
      on-profile: dev-postgres
  datasource:
    url: jdbc:postgresql://${DB_BIZTREE_HOST:localhost}:${DB_BIZTREE_PORT:5432}/${DB_BIZTREE_NAME:esq2025}
    username: ${DB_BIZTREE_USERNAME:esq2025}
    password: ${DB_BIZTREE_PASSWORD:q}
    hikari:
      maximum-pool-size: 20
      minimum-idle: 20
      connection-timeout: 30000
      max-lifetime: 1800000
      idle-timeout: 600000
  jpa:
    mapping-resources:
      - META-INF/postgres-entity.xml
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: ${BIZTREE_PORT:3000}
