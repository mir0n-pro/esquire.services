apiVersion: v2
name: esquire-aukeep
description: auKeep — the audit deployment of the dataKeep keep service (drains the audit bus, writes the *_log tables)
type: application
version: 0.1.0
appVersion: "1.2.8"
