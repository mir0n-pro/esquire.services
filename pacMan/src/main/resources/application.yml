spring:
  profiles:
    active: ${DB_PACMAN_VENDOR:dev-postgres}
  application:
    name: pacman
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
  mvc:
    problemdetails:
      enabled: true
  config:
    import: "${ESQUIRE_TOPOLOGY_IMPORT:file:/etc/esquire/topology.yml}"
# Bus TOPOLOGY (which buses exist + their transport) is the shared esquire-topology.yml imported above.
# Here: this service's bus REFS + per-service knob OVERRIDES, and the service-LOCAL audit (b) in-process leg.
esquire:
  pacman-messaging-bus:
    - bus-id: audit-b
      slot:
        - slot-id: audit
          x-rod:
            rod-class: pro.mir0n.esquire.common.audit.XRodLogDb
            pool-size: ${ESQUIRE_AUDIT_POOL_SIZE:4}
            feed-capacity: ${ESQUIRE_AUDIT_FEED_CAPACITY:4096}
            virtual-threads: ${ESQUIRE_AUDIT_VIRTUAL_THREADS:false}
            log-db:
              vendor: ${ESQUIRE_AUDIT_LOG_DB_VENDOR:dev-postgres}
              url: ${ESQUIRE_AUDIT_LOG_DB_URL:jdbc:postgresql://${DB_PACMAN_HOST:localhost}:${DB_PACMAN_PORT:5432}/${DB_PACMAN_NAME:esq2025}}
              username: ${ESQUIRE_AUDIT_LOG_DB_USERNAME:${DB_PACMAN_USERNAME:esq2025}}
              password: ${ESQUIRE_AUDIT_LOG_DB_PASSWORD:${DB_PACMAN_PASSWORD:q}}
              pool-size: ${ESQUIRE_AUDIT_LOG_DB_POOL_SIZE:2}
  audit-bus:
    messaging-bus:
      bus-id: ${ESQUIRE_AUDIT_BUS_ID:audit-b}
      slot-id: ${ESQUIRE_AUDIT_SERVICE_ID:audit}
  entity-bus:
    messaging-bus:
      bus-id: ${ENTITY_BUS_ID:esquire.entity}
      slot-id: ${ENTITY_SERVICE_ID:entity}
      x-rod:
        publisher-pool-size: ${ENTITY_BROADCAST_PUBLISHER_POOL_SIZE:0}
logging:
  level:
    root: ${LOG_LEVEL_ROOT:ERROR}
    org:
      springframework: ${LOG_LEVEL_SF:ERROR}
      springframework.jms: ${LOG_LEVEL_JMS:INFO}
      apache:
        activemq: ${LOG_LEVEL_AMQ:INFO}
    pro:
      mir0n: ${LOG_LEVEL_MIR0N:ERROR}
    develop: ${LOG_LEVEL_DEVELOP:DEBUG}
    msg: ${LOG_LEVEL_MSG:INFO}
  develop:
    log-path: ${DEVELOP_LOG_PATH:logs/pacMan-develop.log}
  msg:
    log-path: ${MSG_LOG_PATH:logs/pacMan-msg.log}

---
spring:
  config:
    activate:
      on-profile: dev-oracle
  datasource:
    url: jdbc:oracle:thin:@//${DB_PACMAN_HOST:localhost}:${DB_PACMAN_PORT:1521}/${DB_PACMAN_NAME:MIR0N}
    driverClassName: oracle.jdbc.OracleDriver
    username: ${DB_PACMAN_USERNAME:esq2025}
    password: ${DB_PACMAN_PASSWORD:q}
    hikari:
      maximum-pool-size: 20
      minimum-idle: 20
      connection-timeout: 30000
      max-lifetime: 1800000
      idle-timeout: 600000
  jpa:
    mapping-resources:
      - META-INF/oracle-acct.xml
      - META-INF/oracle-acct-transaction.xml
      - META-INF/oracle-roles.xml
    properties:
      hibernate:
        dialect: org.hibernate.dialect.OracleDialect
server:
  port: ${PACMAN_PORT:3000}

---
spring:
  config:
    activate:
      on-profile: dev-postgres
  datasource:
    url: jdbc:postgresql://${DB_PACMAN_HOST:localhost}:${DB_PACMAN_PORT:5432}/${DB_PACMAN_NAME:esq2025}
    username: ${DB_PACMAN_USERNAME:esq2025}
    password: ${DB_PACMAN_PASSWORD:q}
    hikari:
      maximum-pool-size: 20
      minimum-idle: 20
      connection-timeout: 30000
      max-lifetime: 1800000
      idle-timeout: 600000
  jpa:
    mapping-resources:
      - META-INF/postgres-acct.xml
      - META-INF/postgres-acct-transaction.xml
      - META-INF/postgres-roles.xml
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: ${PACMAN_PORT:3000}
