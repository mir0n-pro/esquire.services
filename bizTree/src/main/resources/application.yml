spring:
  profiles:
    active: ${DB_BIZTREE_VENDOR:dev-postgres},${BIZTREE_CACHE_VENDOR:cache-h2}
  application:
    name: biztree
  # v1.2.10 VT: run each REST request on a virtual thread (Tomcat); opt-in, default OFF (pre-HA). See HA doc 5.5.
  threads:
    virtual:
      enabled: ${ESQ_VIRTUAL_THREADS:false}
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
  mvc:
    problemdetails:
      enabled: true
  # Request-path query/transaction cap (seconds): -1 = no cap (pre-HA default; behaviour unchanged when unset).
  # Set ESQ_TX_TIMEOUT_S (e.g. 8) to bound the common request path. The full-tree cache load opts out via
  # biztree.cache-load.tx-timeout-s below; the in-memory H2 cache has its own cap (biztree.h2.query-timeout-s).
  transaction:
    default-timeout: ${ESQ_TX_TIMEOUT_S:-1}
  config:
    import: "${TOPOLOGY_IMPORT:optional:file:/etc/esquire/topology.yml}"
biztree:
  # cache director + monad/taijitu knobs (read by @Value; surfaced here for env visibility -- defaults
  # match the @Value fallbacks, so behaviour is unchanged when the env var is unset).
  director: ${BIZTREE_DIRECTOR:legacy}
  # The startup cache load (whole org/usr/acct read) opts OUT of the request-path cap: 0 = uncapped (pre-HA
  # default). Set a positive value only to put a safety ceiling on a full-tree load.
  cache-load:
    tx-timeout-s: ${BIZTREE_CACHE_LOAD_TX_TIMEOUT_S:0}
  monad:
    queue:
      capacity: ${BIZTREE_MONAD_QUEUE_CAPACITY:4096}
  queue:
    bulk-threshold: ${BIZTREE_QUEUE_BULK_THRESHOLD:10}
  taijitu:
    on-mismatch: ${BIZTREE_TAIJITU_ON_MISMATCH:LOG}
    sweep:
      interval-ms: ${BIZTREE_TAIJITU_SWEEP_INTERVAL_MS:10000}
      timeout-ms: ${BIZTREE_TAIJITU_SWEEP_TIMEOUT_MS:10000}
  h2:
    url: ${BIZTREE_H2_URL:jdbc:h2:mem:biztree;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE}
    # per-statement cap on the in-memory cache (seconds): 0 = uncapped (pre-HA default). The H2 cache surface
    # of the request-path cap; the cache replies fast, so this only guards a pathological cache statement.
    query-timeout-s: ${BIZTREE_H2_QUERY_TIMEOUT_S:0}
    pool:
      pool-name:            biztree-h2-cache
      maximum-pool-size:    ${BIZTREE_H2_POOL_MAX:10}
      minimum-idle:         ${BIZTREE_H2_POOL_MIN_IDLE:10}
      connection-timeout:   ${BIZTREE_H2_POOL_CONN_TIMEOUT:5000}
      max-lifetime:         ${BIZTREE_H2_POOL_MAX_LIFETIME:1800000}
      idle-timeout:         ${BIZTREE_H2_POOL_IDLE_TIMEOUT:600000}
# Bus TOPOLOGY (which buses exist + their transport) is the shared esquire-topology.yml imported above.
# Here: this service's bus REF + per-service knob OVERRIDE (entity consumer).
esquire:
  # Distributed tracing (v1.2.11 O2) via common.TracingConfig. Off by default = zero cost; the
  # collector endpoint + sampling ratio are set per environment (compose / k8s).
  tracing:
    enabled: ${ESQ_TRACING_ENABLED:false}
    otlp-endpoint: ${ESQ_OTLP_ENDPOINT:http://localhost:4318/v1/traces}
    sampling-ratio: ${ESQ_TRACING_SAMPLING_RATIO:1.0}
    marks-enabled: ${ESQ_TRACING_MARKS_ENABLED:true}
    excluded-paths: ${ESQ_TRACING_EXCLUDED_PATHS:/actuator}
  entity-bus:
    messaging-bus:
      bus-id: ${ENTITY_BUS_ID:esquire.entity}
      slot-id: ${ENTITY_SLOT_ID:entity}
      role: CLIENT
      x-rod:
        receiver-pool:
          size: ${ENTITY_BROADCAST_POOL_SIZE:4}
          mode: ${ENTITY_BROADCAST_POOL_MODE:platform}   # platform | virtual | virtual-per-task
        concurrency: ${ENTITY_BROADCAST_CONCURRENCY:1}
logging:
  level:
    root: ${LOG_LEVEL_ROOT:ERROR}
    org:
      springframework: ${LOG_LEVEL_SF:ERROR}
      springframework.jms: ${LOG_LEVEL_JMS:ERROR}
      apache:
        activemq: ${LOG_LEVEL_AMQ:ERROR}
    pro:
      mir0n: ${LOG_LEVEL_MIR0N:INFO}
    develop: ${LOG_LEVEL_DEVELOP:DEBUG}
    msg: ${LOG_LEVEL_MSG:INFO}
  develop:
    log-path: ${DEVELOP_LOG_PATH:logs/bizTree-develop.log}
  msg:
    log-path: ${MSG_LOG_PATH:logs/bizTree-msg.log}
management:
  # Micrometer-Tracing bridge is active only when tracing is enabled; the OTel exporter + sampler are
  # still declared explicitly by common.TracingConfig (mirrors esquire.tracing.enabled).
  tracing:
    enabled: ${ESQ_TRACING_ENABLED:false}
  endpoint:
    health:
      probes:
        enabled: true               # expose the liveness + readiness health groups
      show-details: always
      # messagingBus is registered DYNAMICALLY at ApplicationReadyEvent (after the membership-validating context
      # refresh), so the startup check must be off; the readiness group resolves it at request time.
      validate-group-membership: false
      group:
        # bus health depools the pod (READINESS), never liveness -- a broker outage must not let liveness kill it.
        readiness:
          include: readinessState, cacheReadiness, messagingBus

---
# Development Oracle
spring:
  config:
    activate:
      on-profile: dev-oracle
  datasource:
    url: jdbc:oracle:thin:@//${DB_BIZTREE_HOST:localhost}:${DB_BIZTREE_PORT:1521}/${DB_BIZTREE_NAME:MIR0N}
    driverClassName: oracle.jdbc.OracleDriver
    username: ${DB_BIZTREE_USERNAME:esq2025}
    password: ${DB_BIZTREE_PASSWORD:q}
    hikari:
      # pool sizing + connect-timeout: pre-HA defaults = current values; HA values (smaller pool, fast
      # connect-timeout) set per service in the local-k8s chart (R3/R5).
      maximum-pool-size: ${ESQ_DB_POOL_MAX:20}
      minimum-idle: ${ESQ_DB_POOL_MIN_IDLE:20}
      connection-timeout: ${ESQ_DB_CONNECT_TIMEOUT_MS:30000}
      max-lifetime: 1800000
      idle-timeout: 600000
  jpa:
    mapping-resources:
      - META-INF/oracle-entity.xml
    properties:
      hibernate:
        dialect: org.hibernate.dialect.OracleDialect
server:
  port: ${BIZTREE_PORT:3000}
  tomcat:
    threads:
      # request worker threads (pre-HA default = Boot's 200). HA bounds the worker pool so a slow
      # downstream cannot exhaust threads fleet-wide; local-k8s value set in the chart (R2).
      max: ${ESQ_TOMCAT_MAX_THREADS:200}
    accept-count: ${ESQ_TOMCAT_ACCEPT_COUNT:100}

---
# Development Postgres
spring:
  config:
    activate:
      on-profile: dev-postgres
  datasource:
    url: jdbc:postgresql://${DB_BIZTREE_HOST:localhost}:${DB_BIZTREE_PORT:5432}/${DB_BIZTREE_NAME:esq2025}
    username: ${DB_BIZTREE_USERNAME:esq2025}
    password: ${DB_BIZTREE_PASSWORD:q}
    hikari:
      # pool sizing + connect-timeout: pre-HA defaults = current values; HA values (smaller pool, fast
      # connect-timeout) set per service in the local-k8s chart (R3/R5).
      maximum-pool-size: ${ESQ_DB_POOL_MAX:20}
      minimum-idle: ${ESQ_DB_POOL_MIN_IDLE:20}
      connection-timeout: ${ESQ_DB_CONNECT_TIMEOUT_MS:30000}
      max-lifetime: 1800000
      idle-timeout: 600000
      # pgjdbc socketTimeout (s) + tcpKeepAlive: pre-HA defaults = off (0 / false). HA (local-k8s chart)
      # makes a vanished DB fail fast on a half-open socket instead of hanging a worker (R1/R5).
      data-source-properties:
        socketTimeout: ${ESQ_DB_SOCKET_TIMEOUT_S:0}
        tcpKeepAlive: ${ESQ_DB_TCP_KEEPALIVE:false}
  jpa:
    mapping-resources:
      - META-INF/postgres-entity.xml
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: ${BIZTREE_PORT:3000}
  tomcat:
    threads:
      # request worker threads (pre-HA default = Boot's 200). HA bounds the worker pool so a slow
      # downstream cannot exhaust threads fleet-wide; local-k8s value set in the chart (R2).
      max: ${ESQ_TOMCAT_MAX_THREADS:200}
    accept-count: ${ESQ_TOMCAT_ACCEPT_COUNT:100}
