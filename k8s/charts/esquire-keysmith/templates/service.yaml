apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-keysmith
  labels:
    app: {{ .Release.Name }}-keysmith
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}-keysmith
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-keysmith-hl
  labels:
    app: {{ .Release.Name }}-keysmith
spec:
  clusterIP: None
  selector:
    app: {{ .Release.Name }}-keysmith
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
