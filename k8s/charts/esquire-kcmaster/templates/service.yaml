apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-kcmaster
  labels:
    app: {{ .Release.Name }}-kcmaster
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}-kcmaster
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-kcmaster-hl
  labels:
    app: {{ .Release.Name }}-kcmaster
spec:
  clusterIP: None
  selector:
    app: {{ .Release.Name }}-kcmaster
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
