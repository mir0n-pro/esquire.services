apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-enyman-secret
  labels:
    app: {{ .Release.Name }}-enyman
type: Opaque
data:
  db-password: {{ .Values.db.password | b64enc | quote }}
