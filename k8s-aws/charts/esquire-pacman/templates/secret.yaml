apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-pacman-secret
  labels:
    app: {{ .Release.Name }}-pacman
type: Opaque
data:
  db-password: {{ .Values.db.password | b64enc | quote }}
  mq-password: {{ .Values.mq.password | default "" | b64enc | quote }}
