<table style="width: 100%; table-layout: fixed;">
  <tr>
    <td style="width: 12%"><img src="../favicon.ico" alt="Esquire logo" align="right" valign="middle" width="64"></td>
    <td style="width: 88%;">
       <h1>Esquire Application Frameworks(tm) 2.0</h1>
    </td>
  </tr>
</table>

# v1.2.x Planning

Planning document for the v1.2.x release line — its themes, per-sprint scope, and cross-repository progress. The line is now **complete**; forward directions live in the continuous-development backlog.

Scope: all four Esquire repositories — esquire.services (this repo, project facade), esquire.explorer, esquire.ui.lib, esquire.db.seed.

---

## Release line so far

| Version | Date | Headline |
|---|---|---|
| **v1.2.2** | 2026-04-22 | The Establishment — first complete vertical slice; schema, services, UI library, frontend; live demo at https://esquire.mir0n.pro |
| **v1.2.3** | 2026-05-08 | BFF sprint — Backend-for-Frontend tier; browser holds opaque session cookie; gateway becomes ClusterIP-only; KC moves to `/kc-auth`; standalone frontend retired |
| **v1.2.4** | 2026-05-14 | Hauberk + non-browser auth-patterns sprint — Gatling Java DSL load harness; two new gateway auth patterns (Vanilla Token Relay, Phantom Token Relay via RFC 8693); realm-level access-token slim; outer/inner observability timer protocol; race-condition repros (fixes deferred to v1.2.5+) |
| **v1.2.5** | 2026-05-24 | bizTree cache refactor — the "Taijitu" Supreme Ultimate Cache; anti-entropy double-buffer + night-watch sweep; closes the v1.2.4 cache-load race behind an unchanged API surface |
| **v1.2.6** | 2026-06-02 | enyMan redundancy / race-8c sprint — instance-aware entity-id minting; async move queue; kcMaster path-buffer; bizTree event work-batching (~3x broadcast-apply throughput); race-8b/8c fixes |
| **v1.2.7** | 2026-06-10 | audit-logging + CI/CD sprint — pluggable audit seam; generic **x-rod** fan-out substrate + a standalone audit-writer consumer service; six strategies over ActiveMQ/Kafka/Redis; **CI/CD pipeline established** (GitHub Actions: CI + local-k8s deploy + approved OKE release) — first automated production release |
| **v1.2.8** | 2026-06-19 | Messaging Bus sprint — the messaging layer lifted into a vendor-agnostic **Messaging Bus** subframework (`esquire-messaging` + pluggable transport-provider drivers `tp-activemq` / `tp-kafka` / `tp-redis`); `esquire-audit` / `esquire-data-keep` split out of `common`; the audit-writer `xx-rod` renamed **auKeep**; **system-entity anti-deletion flag** (the standalone access log postponed) |
| **v1.2.9** | 2026-06-24 | Hardening sprint — the Messaging Bus given one per-service front-door + a defined start-up lifecycle, configuration-driven roles, and fail-fast on misconfiguration; bus-connection health on every service's health check (readiness/liveness split) with an opt-in keep-alive heartbeat; defensive message decoding; DB timestamp columns + lookup indexes; the bus wire constants lifted into `esquire-messaging` (`BusConstants`) |
| **v1.2.10** | 2026-07-05 | Resilience / Durability sprint — OKE high-availability deploy (x2 replicas spread across nodes / availability domains); the R1–R5 resilience budget (bounded Tomcat + Hikari pools, fail-fast DB socket timeout + keep-alive, a per-route query-timeout cap); the messaging-path send-retry sublayer + keep-alive; message loss-visibility |
| **v1.2.11** | 2026-07-25 | Observability sprint — the open-source observability stack (Grafana over Prometheus metrics / Loki logs / Tempo traces through an OTel Collector), all correlated on one id; per-service + business meters (`esq.biz.*`); a live topology board; on-demand, transient enablement (off by default, including on OKE) |
| **v1.2.12** | 2026-08-11 | Continuing dev — per-entity **change number**; a strictly-increasing per-entity counter that simplifies audit dedup, event ordering, and optimistic concurrency at once |
| **v1.2.13** | 2026-08-27 | Continuing dev — **compact topology and hardening**; the same code and settings composed into fewer programs (Mesnie, gateWard), and the solution read back item by item — the open findings from the "clear-mind" review passes closed |

Per-repo milestone reports for every sprint are in [Releases.md](../Releases.md#milestone-reports).

---

## Theme — the v1.2.x horizon

The v1.2.x line set out to take the framework first stood up in v1.2.2 — the complete vertical slice on the traditional stack (PostgreSQL + microservices + JPA + REST + Node.js + Kubernetes) — and apply that same stack to the solutions postponed in v1.2.2, filling the gaps that deep testing and architecture work surfaced. In short: do all possible to let the framework cover the full stack.

**With v1.2.11 — active development of the v1.2.x horizon is COMPLETE.** The framework covers the full stack it set out to. It now moves into **support / continuous-development mode**: work is drawn from the [continuous-development backlog](Esquire.ContinuingDev.md) as candidate sprint targets, and several can run in parallel rather than one strict sequential line.

**Continuing development, sprint by sprint.** The line is not a plan being worked through; it expands and improves one sprint at a time, each target picked from the backlog when the sprint opens:

- **v1.2.12 — per-entity change number** (delivered 2026-08-11). A strictly-increasing change-no per entity is a cross-cutting gap that simplifies several layers at once — audit dedup, event ordering, optimistic concurrency — so it is the first to pick up.
- **v1.2.13 — compact topology and hardening** (delivered 2026-08-27). Two sprints' worth in one: the framework composed into fewer programs — Mesnie carrying the entity, identity and sign-in work, gateWard the gate and the tree cache — so the same code and the same settings run as eight programs, as five, or as four; and then the whole thing read back item by item, which closed the open findings from the "clear-mind" review passes and turned up a further set of its own.
- **v1.2.14 — AWS** (opened 2026-08-28). Esquire on AWS: deployed on EKS, and the Messaging Bus extended with the brokers AWS provides — SNS carrying the entity broadcast, SQS the identity request/response, Kinesis the audit log — through the same transport-provider seam that already carries ActiveMQ, Kafka and Redis. Mostly integration and deployment rather than new development. Amazon Cognito and AWS CloudWatch are studied and written up, not adopted; native AWS observability is out of scope. The plan is [tasks1214.md](plans/tasks1214.md).

After this one there is no fixed next sprint. What follows is drawn from the [continuing-development backlog](Esquire.ContinuingDev.md) when the sprint opens, and more than one can run at a time, each against its own target.

Beyond the v1.2.x line, **v1.3.0** externalizes the Messaging Bus into its own product / repository.

---

## Sprint cadence

### Version scheme

Esquire releases follow the form **`vMajor.Minor.Micro-YYMM.DDHH`**:

- **Major** (`1`) — generation of the framework.
- **Minor** (`2`) — horizon within the generation.
- **Micro** (`3`) — sprint within the horizon. Conventionally this digit is used as a build counter; in Esquire it identifies a sprint instead.
- **Build extension** (`-YYMM.DDHH`, EST timezone) — identifies an intermediate commit *within* the sprint. Each meaningful code change during the sprint advances this stamp; it is not a recompile counter for unchanged code. `DDHH` is day-of-month + hour-of-day.

So `v1.2.3-2605.0512` reads as: *generation 1, second horizon, third sprint, intermediate commit at 12:00 EST on the 5th of May 2026*.

### Cadence rule

Within the v1.2.x horizon, every system modification is organized as a **sprint with its own Micro version**. Each sprint has one headline scope item — the reason for the version bump — and may fold in **minor unrelated changes** that were ready at the same time and are too small to justify their own sprint.

Example: **v1.2.3** is the BFF sprint. Mobile pointer-device support in `esquire.ui.lib` (a small unrelated change ready alongside) is folded into the same v1.2.3 release rather than waiting.

This keeps the versioning meaningful (the headline tells you what the sprint is *about*) while letting the release stream remain frictionless for incremental improvements.

### Branch flow

Each sprint follows the same git workflow across all four repositories:

1. Work happens on `pending-v1.2.X`.
2. When the sprint is ready: PR / merge `pending-v1.2.X` → `develop`.
3. Finalize on `develop` (release notes, `changes.txt`, source-file history headers, version stamp).
4. Tag `v1.2.X` on `develop`.
5. Create archive branch `release/v1.2.X` from the tagged commit.
6. Create the next sprint's `pending-v1.2.(X+1)` branch from `develop`.
7. Delete the old `pending-v1.2.X` branch.

---

## Non-sprint continuous activity

These run alongside every sprint as routine work, not as headline scope. They do not earn their own Micro version.

### Documentation suite

> "No one needs undocumented frameworks."  _Salvatore Sanfilippo (Antirez)_  

The documentation suite is itself a framework feature. Guides, tutorials, and AI prompts for Esquire-based implementations are continuous deliverables — defined and grown as required, not gated on a sprint. The concrete set of docs that constitutes the framework's documentation feature is to be established as the need surfaces.

Think about an **"Esquire Manifesto"** — the front matter of the documentation suite, the framework's principles stated as a handful of declarative tenets a reader can absorb in thirty seconds. Sketch:

1. **The tree is the authorization model.** The org chart and the access rules are the same artifact, not two artifacts that drift apart.
2. **Position grants visibility. Position grants authority.** Authority is not a global property of an account; it is a function of where you sit.
3. **Every entity has a kind.** Kind is the type — it determines structure, validation, available commands, and rendering. Behavior follows kind, not class hierarchy.
4. **Every entity has one ID, globally unique.** Across kinds, across branches, across services — an entity ID identifies exactly one entity. A reference is one number, never a `(kind, id)` pair.
5. **The entity defines itself.** Field layout, validation rules, available commands — declared once at the business layer; the UI listens at runtime.
6. **Solve the floor once.** The structural plumbing of every backoffice — users, permissions, audit, accounts, admin UI — is solved once so the product can be the actual point.
7. **No vendor stands between you and your data.** Database, broker, IAM, cloud — every external dependency sits behind a swappable adapter. Open source. Self-hostable. Yours.

### Testing

**Gatling (Java DSL)** is Esquire's standard for integration, stress, and load testing;
its Simulation set lives in the **hauberk** module. Any sprint, for any service, builds
new Chains and Simulations on that same foundation instead of adding a second framework.
Reference: `doc/Esquire.TestingStack.md`.

This is a continuous activity rather than a sprint headline: any sprint can grow the
testing surface (new Chains, new Simulations, new assertions) as a routine deliverable
alongside its primary scope, the same way each sprint maintains documentation.

